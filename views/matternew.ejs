<%- include('bootheader') -%>
<%- include('menubar') -%>
    <div class="jumbotron">
      <h1>Create New Days Matter</h1>
      <p>Update page</p>
    </div>

    <p><img src="images/lute.jpg" width="30%"></p>

    <form method="post" action="/matternew">
      New Days Matter: <br>
      <textarea id="post" name="post"></textarea>
      <br>
      <input type="submit">
    </form>
    <hr>
    <form method="post" action="/matterDelete">
        <input type="submit" value="delete marked">
        <ol>
        <% for(i = 0; i<posts.length; i++){  %>
            <li> <a href="/showPost/<%= posts[i]._id %>">
                  <%= posts[i].userName %></a>
                  <i>
                 <%= posts[i].createdAt %></\/i> <br>
                 <% console.dir([posts[i].userId,user._id]) %>
                 <% if (posts[i].userId.equals(user._id)) { %>
                     <input type="checkbox" name="delete" value="<%= posts[i]._id %>">
                 <% } %>
                 <pre><%= posts[i].post %></pre>

             </li>
        <% } %>
        </ol>

    </form>

<%- include('bootfooter') -%>
